---
import Icons, { type IconsType } from "@/low-components/icons";

interface Props {
  name: keyof IconsType;
  class?: string;
  id?: string;
}
const { id, name } = Astro.props;
const { body, viewBox = "0 0 24 24" } = Icons[name];
---

<svg id={id} class={Astro.props.class} viewBox={viewBox} set:html={body} />
