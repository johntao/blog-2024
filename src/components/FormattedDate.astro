---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="inline-block w-32">
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: '2-digit',
		})
	}
</time>
